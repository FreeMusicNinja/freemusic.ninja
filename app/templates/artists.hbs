<div>
  <h3>Artist Lookup</h3>
  <p>Type the name of an artist with Creative Commons-licensed music to find their websites.</p>
  {{input type="text" value=query action="query"}}
  {{#each results}}
    <h3>{{name}}</h3>
    <ul>
    {{#if jamendo_url}}
      <li><a {{bind-attr href=jamendo_url}}>Jamendo</a></li>
    {{/if}}
    {{#if magnatune_url}}
      <li><a {{bind-attr href=magnatune_url}}>Magnatune</a></li>
    {{/if}}
    </ul>
  {{else}}
  <p>No artist found.</p>
  {{/each}}
</div>
