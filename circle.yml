machine:
  python:
    version: 3.4.0

dependencies:
  override:
    - pip install -r django/requirements.txt:
        timeout: 60

test:
  override:
    - python django/manage.py test:
        timeout: 60
